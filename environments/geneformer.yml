name: geneformer
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - "geneformer @ git+https://huggingface.co/ctheodoris/Geneformer.git@ad8f66d"
# ad8f66d is the checkpoint the manuscript reports. Do not install from
# geneformer-pip-freeze.txt: it is a provenance snapshot with local paths.
